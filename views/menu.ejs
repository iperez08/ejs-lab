<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
</head>
<%- include('./partials/nav') %>
    <body>
        <h1>Full Menu</h1>
        <% menu.forEach((item)=> { %>
            <p><%= item.name %> </p>
            <ul type="none">
                <li> Price: <%= item.price %></li>
                <li> Rating: <%= item.rating %></li>
                <li> Deets: <%= item.details %></li>
            </ul>
            <% }) %>
    </body>

</html>